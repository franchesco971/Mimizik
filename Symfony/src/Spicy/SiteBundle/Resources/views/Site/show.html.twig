{% extends "SpicySiteBundle::layout.html.twig" %}
{% block title %}
{{ parent() }} : {{video.getNomArtistes}} - {{video.titre}}
{% endblock %}

{% block body %}
    {{ parent() }}

    <div id="video">
        <div>
            
            <h2 >
                <span class="artisteVideo">
                {% for artiste in video.artistes %}
                    <a href="{{path('spicy_site_artiste',{'id':artiste.id})}}">{{artiste.libelle}}</a>    
                    {% if loop.last %}

                    {% elseif loop.first %} & {% else %} , {%endif%}
                {% endfor %}
                </span>
                  - 
                <a href="{{path('spicy_site_video_slug',{'id':video.id,'slug':video.slug})}}">
                    {{video.titre}}
                </a>
            </h2>
            
        </div>
	<!--<iframe width="560" height="315" src="http://www.youtube.com/embed/<?php echo $video->url() ?>?autoplay=1" frameborder="0" allowfullscreen></iframe>-->
	<!--<iframe width="900" height="506" src="http://www.youtube.com/embed/{#{video.url}#}?autoplay=1" frameborder="0" allowfullscreen></iframe>-->
        <object width="900" height="506">
            <param name="movie" value="http://www.youtube.com/v/{{video.url}}?version=3&amp;hl=fr_FR&autoplay=1"></param>
            <param name="allowFullScreen" value="true"></param><param name="allowscriptaccess" value="always"></param>
            <embed src="http://www.youtube.com/v/{{video.url}}?version=3&amp;hl=fr_FR&autoplay=1" type="application/x-shockwave-flash" width="900" height="506" allowscriptaccess="always" allowfullscreen="true"></embed>
        </object>
	<!--<form name="n" action="post">
		<p>
			<label for="textfield2">Commentaire</label><br>
			<input type="text" name="contenu" id="contenu">dadzaz
		</p>
		<p> 
			<input type="submit" name="Submit" value="Commenter">
		</p>
	</form>-->
        <div id="fb_infos" class="fond_transparent_blanc">
            <div id="social" >
                <div class="fb-like" data-href="{{website_name}}{{path('spicy_site_video_slug',{'id':video.id,'slug':video.slug})}}" data-send="true" data-width="450" data-show-faces="true"></div>
                <!--<g:plusone size="tall"></g:plusone>-->
               <!-- <a title="Partager sur Facebook" 
                   
                   href="https://www.facebook.com/dialog/feed?app_id=458358780877780&link={{website_name}}{{path('spicy_site_video_slug',{'id':video.id,'slug':video.slug})}}&redirect_uri=https://www.facebook.com/dialog/feed?app_id=458358780877780&link={{website_name}}{{path('spicy_site_video_slug',{'id':video.id,'slug':video.slug})}}&display=popup&show_error=true&picture=http://img.youtube.com/vi/{{video.url}}/0.jpg&name=Mimizik.com"
                   >
                    <img src="{{asset('images/petit_logo.png')}}" alt="Partager sur Facebook" />
                </a>-->
                <!--href="https://www.facebook.com/dialog/feed?app_id=458358780877780&link={{website_name}}{{path('spicy_site_video_slug',{'id':video.id,'slug':video.slug})}}&picture=http://img.youtube.com/vi/{{video.url}}/0.jpg&name={{video.getNomArtistes}} - {{video.titre}}&caption={{video.getNomArtistes}} - {{video.titre}}&description={{video.getNomTypes}}&redirect_uri=&display=popup&show_error=true" -->
            </div>
            <div class="infos">
                <p >Artistes: 
                    {% for artiste in video.artistes %}
                    <a href="{{path('spicy_site_artiste',{'id':artiste.id})}}">{{artiste.libelle}}</a>    
                    {% if loop.last %}

                    {% elseif loop.first %} & {% else %} , {%endif%}
                {% endfor %}
                </p>
                <p>Genres: 
                    {#{video.getNomGenres}#}
                    {% for genre in video.genreMusicaux %}
                    <a href="{{ path('spicy_site_genre',{'id':genre.id,'slug':genre.slug})}}">{{genre.libelle}}</a>    
                    {% if loop.last %}

                    {% elseif loop.first %} & {% else %} , {%endif%}
                {% endfor %}
                </p>
                <p>Types de video: {{video.getNomTypes}}</p>
                <p>Source: {% if video.getSource %}<a href="{{video.getTxtSource}}">{{video.getSource}}</a>{% else %}Inconnu{% endif %}</p>
            </div>
        </div>

	<div class="fb-comments fond_transparent_blanc" data-href="{{domaine_name}}{{path('spicy_site_video_slug',{'id':video.id,'slug':video.slug})}}" data-width="470" data-num-posts="2"></div>

</div>
{% endblock %}
