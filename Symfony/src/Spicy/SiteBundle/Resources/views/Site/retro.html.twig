<h5>Chaque semaines retrouvez la vidéo rétro de la semaine. 
    Par rétro on entend un titre qui n'est pas récent. Cette video peur être de tous genres.
</h5><br/>
{% if video is defined %}
    <div class="image" >
        <a href="{{path('spicy_site_video_slug',{"id":video.id,"slug":video.slug})}}">
            <img alt="" src="http://img.youtube.com/vi/{{video.url}}/0.jpg" width="240" height="135" />
        </a>
    </div>
    <div class="description">{{video.description}}</div>
{%else%}
    pas de resultat
{%endif%}
