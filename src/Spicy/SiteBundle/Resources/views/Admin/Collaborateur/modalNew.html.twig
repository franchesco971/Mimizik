{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hashtag creation</h1>

    {{ form_start(form, {'method': 'POST', 'action': path('admin_collaborateur_create_modal')}) }}
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    {{ form_end(form) }}
{% endblock %}

