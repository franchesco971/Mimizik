{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hashtag creation</h1>

    <form action="{{ path('admin_hashtag_create_modal_update',{'id': idArtiste})}}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <!--<ul class="record_actions">
    <li>
        <a href="{{ path('admin_hashtag') }}">
            Back to the list
        </a>
    </li>
</ul>-->
{% endblock %}

